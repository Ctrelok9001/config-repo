server:
  port: 8085

spring:
  application:
    name: game-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG

custom:
  greeting: "Hello from Game Service"
